---
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      github-actions:
        # Group all GitHub Actions updates together
        patterns:
          - "*"
    cooldown:
      default-days: 7
